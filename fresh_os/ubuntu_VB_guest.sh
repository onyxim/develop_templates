#!/usr/bin/env bash
# Install this to support ubuntu as guest inside VB
### After install this utils shared folders will works in guest Ubuntu
sudo apt-get update && sudo apt-get upgrade
sudo apt-get install virtualbox-guest-utils